import styled from 'styled-components';

export const StyledGenreBtn = styled.button`
  padding: 0.9rem 2rem;
  border: 1px solid var(--color-grey-2);
  border-radius: 2rem;
  background: transparent;
`;
